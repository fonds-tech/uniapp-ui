# UniApp UI

跨端 UI 组件库 - 基于 Vue 3 + UniApp，支持 H5、小程序、App 多端适配。

## ✨ 特性

- 🚀 **开箱即用** - 80+ 精心设计的 UI 组件，覆盖移动端常见业务场景
- 📦 **按需引入** - 支持 easycom 自动导入，无需手动注册组件
- 🎨 **主题定制** - 基于 CSS 变量的主题系统，轻松实现个性化定制
- 📱 **多端适配** - 一套代码，同时支持 H5、微信小程序、App 等多端运行
- 🔧 **TypeScript** - 使用 TypeScript 编写，提供完整的类型定义
- ⚡ **高性能** - 优化渲染性能，确保流畅的用户体验

## 📦 安装

### 环境要求

- Node >= 20
- pnpm >= 9

### 安装依赖

```bash
pnpm install
```

## 🚀 快速开始

### 开发模式

```bash
# H5 开发
pnpm dev:h5

# 微信小程序开发
pnpm dev:mp-weixin

# App 开发
pnpm dev:app
```

### 构建

```bash
# H5 构建
pnpm build:h5

# 微信小程序构建
pnpm build:mp-weixin

# App 构建
pnpm build:app
```

## 📖 文档

### 本地运行文档

```bash
# 启动文档开发服务器
pnpm docs:dev

# 构建文档
pnpm docs:build

# 预览文档
pnpm docs:preview
```

## 🧪 测试

```bash
# 运行测试
pnpm test

# 运行单次测试
pnpm test:run

# 测试 UI 模式
pnpm test:ui

# 测试覆盖率
pnpm test:coverage
```

## 📁 目录结构

```
├── docs/                   # VitePress 文档
│   ├── .vitepress/        # VitePress 配置
│   ├── guide/             # 使用指南
│   └── components/        # 组件文档
├── src/
│   ├── ui/                # UI 组件库（核心）
│   │   ├── ui-button/     # Button 组件
│   │   ├── ui-input/      # Input 组件
│   │   └── ...            # 80+ 组件
│   ├── hooks/             # 通用 Hooks
│   ├── utils/             # 工具函数
│   ├── config/            # 配置模块
│   ├── pages/             # 演示页面
│   │   ├── index/         # 首页
│   │   └── demo/          # 组件演示
│   └── style/             # 全局样式
├── tests/                  # 测试文件
│   ├── setup.ts           # 测试环境配置
│   └── unit/              # 单元测试
│       └── ui/            # UI 组件测试
├── vitest.config.ts       # Vitest 配置
└── package.json
```

## 🎨 组件列表

### 基础组件

| 组件    | 描述     |
| ------- | -------- |
| Button  | 按钮组件 |
| Icon    | 图标组件 |
| Image   | 图片组件 |
| Text    | 文本组件 |
| Badge   | 徽标组件 |
| Tag     | 标签组件 |
| Loading | 加载组件 |

### 表单组件

| 组件       | 描述       |
| ---------- | ---------- |
| Input      | 输入框组件 |
| Textarea   | 多行输入框 |
| Checkbox   | 复选框组件 |
| Radio      | 单选框组件 |
| Switch     | 开关组件   |
| Stepper    | 步进器组件 |
| Rate       | 评分组件   |
| Picker     | 选择器组件 |
| DatePicker | 日期选择器 |
| Form       | 表单组件   |

### 反馈组件

| 组件        | 描述         |
| ----------- | ------------ |
| Toast       | 轻提示组件   |
| Dialog      | 对话框组件   |
| Popup       | 弹出层组件   |
| ActionSheet | 动作面板     |
| Notify      | 消息通知组件 |
| Overlay     | 遮罩层组件   |
| Empty       | 空状态组件   |
| Skeleton    | 骨架屏组件   |

### 展示组件

| 组件      | 描述       |
| --------- | ---------- |
| Cell      | 单元格组件 |
| Progress  | 进度条组件 |
| CountDown | 倒计时组件 |
| NoticeBar | 通知栏组件 |
| Swiper    | 轮播图组件 |
| QRCode    | 二维码组件 |

### 导航组件

| 组件       | 描述       |
| ---------- | ---------- |
| Navbar     | 导航栏组件 |
| Tabbar     | 底部标签栏 |
| Tabs       | 标签页组件 |
| Sidebar    | 侧边栏组件 |
| IndexBar   | 索引栏组件 |
| Pagination | 分页组件   |

### 布局组件

| 组件           | 描述         |
| -------------- | ------------ |
| Row/Col        | 栅格布局     |
| Sticky         | 粘性布局组件 |
| SafeAreaTop    | 顶部安全区   |
| SafeAreaBottom | 底部安全区   |

## 🔧 使用方式

### 1. 配置 easycom

在 `pages.config.ts` 中配置：

```typescript
export default defineUniPages({
  easycom: {
    autoscan: true,
    custom: {
      "^ui-(.*)": "@/ui/ui-$1/ui-$1.vue",
    },
  },
})
```

### 2. 在模板中使用

```vue
<template>
  <view>
    <ui-button type="primary">按钮</ui-button>
    <ui-input v-model="value" placeholder="请输入" />
  </view>
</template>
```

## 🛠 开发命令

| 命令                 | 说明           |
| -------------------- | -------------- |
| `pnpm dev:h5`        | H5 开发模式    |
| `pnpm dev:mp-weixin` | 微信小程序开发 |
| `pnpm build:h5`      | H5 构建        |
| `pnpm test`          | 运行测试       |
| `pnpm test:coverage` | 测试覆盖率     |
| `pnpm docs:dev`      | 文档开发模式   |
| `pnpm docs:build`    | 构建文档       |
| `pnpm lint`          | 代码检查       |
| `pnpm type-check`    | 类型检查       |

## 📄 License

[MIT](./LICENSE)
